# Core dependencies for the LLM inference server.
# Install everything with:  pip install -r requirements.txt

# --- HTTP server (M1 / Ajay) ---
fastapi>=0.110,<1.0
uvicorn>=0.27,<1.0
pydantic>=2.0,<3.0

# --- Model & inference (M2 / Harshith) ---
# Uncomment once the real engine work begins.
# torch>=2.1
# transformers>=4.38

# --- Benchmarks & analysis (M5 / Jay) ---
# Uncomment once benchmark work begins.
# matplotlib>=3.8
# pandas>=2.1
# httpx>=0.27          # async load-generator client

# --- Dev / testing (shared) ---
pytest>=8.0
httpx>=0.27           # also used by FastAPI's TestClient
